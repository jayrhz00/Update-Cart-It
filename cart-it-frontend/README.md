# Cart-It web app

React client for Cart-It. See the [**main README**](../README.md) in the repository root for setup, environment variables, and the full project overview.

**Dev:** from this folder, `npm install` then `npm start` (or from repo root: `npm run frontend`).
